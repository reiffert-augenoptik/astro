---
const { caption, href, height, width } = { ...Astro.props };
---

<a class="card" href={href} style={`height: ${height}px; width: ${width}px;`}>
  <div class="card-footer"><p>{caption}</p></div>
  <slot />
</a>

<style>
  .card {
    display: inline-block;
    transition: scale 200ms;
    scale: 1;

    filter: none !important;

    &:hover {
      scale: 1.02;
    }

    img {
      height: 100%;
      width: 100%;
      object-fit: cover;
    }
  }

  .card-footer {
    position: fixed;
    left: 0;
    bottom: 0;
    width: 100%;
    height: 4rem;
    backdrop-filter: blur(5px);
    z-index: 5;
    display: flex;
    flex-direction: column;
    justify-content: center;
    background: color-mix(in hsl, var(--color-bg-content) 65%, transparent);

    p {
      margin: auto;
      font-size: x-large;
      font-weight: bolder;
    }
  }
</style>
